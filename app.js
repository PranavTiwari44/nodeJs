const helpers = require("./helpers");

const total = helpers.sum(10, 200);
console.log("TOTAL: ", total);

// const {sum , mult} = require("./helpers");
// const total = sum(10, 200);
// This is called object destructuring > allows you to simplify code and get the exact method you need from the other class